To start the frontend:
- cd to client/
- run this command in the terminal "npm install && npm run dev"
To start the backend:
- cd to backend/
- run this command in the terminal "composer install"
- run this command in the terminal "php -S localhost:8000"